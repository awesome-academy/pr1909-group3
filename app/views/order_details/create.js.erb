<% if @order.errors.any? || @order_detail.errors.any? %>
  alert("<%= escape_javascript t '.must_login' %>"); 
<% else %>
  $(".cart-text").html("<%= escape_javascript(render 'carts/cart_text') %>")
<% end %>
