<div class="block">
  <%= content_tag(:label) do %>
    <% unless @is_voted %>
      <%= radio_button_tag 'vote_option[id]', option.id %>
    <% end %>
    <%= option.content %>
  <% end %>
  <%= visualize_votes_for option %>
</div>
