show_modal("<%= j render "show" %>", '<%= I18n.t("questions.rep") %>')
subcription = createQuesionChannel(<%= @question.id %>, <%= '0' %>)
$("#modal-shared").on('hidden.bs.modal', function () {
  App.cable.subscriptions.remove(subcription);
})
